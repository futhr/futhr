{
  "name": "waitlist",
  "version": "0.1.0",
  "description": "Multi-brand waitlist Workers for the futhr ventures",
  "private": true,
  "type": "module",
  "license": "SEE LICENSE IN ../../LICENSE.md",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "vite build",
    "dev": "pnpm build && wrangler d1 migrations apply waitlist --local --env local && wrangler dev --env local --ip 127.0.0.1 --port 8787",
    "check": "wrangler types --include-env=false worker-configuration.d.ts && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json && tsc --noEmit --project ./tsconfig.worker.json",
    "icons": "node scripts/icons.ts",
    "test": "pnpm test:unit && pnpm test:workers && pnpm test:e2e",
    "test:unit": "vitest run --config vitest.unit.config.ts",
    "test:workers": "pnpm build && vitest run --config vitest.workers.config.ts",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@cloudflare/vitest-plugin": "1.1.4",
    "@fontsource-variable/archivo": "5.3.0",
    "@playwright/test": "1.62.1",
    "@sveltejs/adapter-cloudflare": "7.2.9",
    "@sveltejs/kit": "2.70.3",
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "@tailwindcss/vite": "4.3.3",
    "@types/node": "24.10.1",
    "svelte": "5.57.0",
    "svelte-check": "4.7.6",
    "tailwindcss": "4.3.3",
    "typescript": "6.0.3",
    "vite": "8.2.2",
    "vitest": "4.1.11",
    "wrangler": "4.128.0"
  }
}
