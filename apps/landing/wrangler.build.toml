# Adapter output only. Never deploy this file: it does not contain the host gate.
name = "project-landings-build"
main = ".svelte-kit/cloudflare/app.js"
compatibility_date = "2026-09-08"
compatibility_flags = ["nodejs_als"]
workers_dev = false
preview_urls = false
[assets]
directory = ".svelte-kit/cloudflare"
binding = "ASSETS"
